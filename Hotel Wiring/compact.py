#This version hasn't been tested!
T = int(input())
for t in range(T):
    M, N, K = [int(x) for x in input().split(" ")]
    hotel =[]
    for f in range(M):
    	floors.append(int(input()))
	hotel.sort()
	recip = (m-k)*n - sum(hotel[0:k])
	rest = sum(hotel [k:n])
	print(recip+rest)